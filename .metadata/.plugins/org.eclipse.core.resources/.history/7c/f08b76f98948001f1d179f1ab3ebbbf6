//이 인터페이스를 구현한 객체는 반드시 알람 기능이 있는 것을 보장해줌
//=> 약속
public interface AlarmSound {
	
	//인터페이스에서는 변수 선언 앞에 public static final이 생략되어 있는 것임
	//인터페이스에서는 static상수를 선언하고 있는 것이다
	int MEMBER_1 = 10;	//-> 상수
	public static final int MEMBER_2 = 11;
	//모든 메서드는 추상메서드임
	//모든 메서드 선언 앞에 사실은 public abstract가 생략되어 있는 것임
	void method1();
	void playAlarm();
}
